---

sudo: false
dist: bionic
language: ruby
cache: bundler

before_install:
  - sudo add-apt-repository ppa:alex-p/tesseract-ocr -y
  - sudo apt-get update -q
  - sudo apt-get install tesseract tesseract-lang -y
  - gem install bundler -v 1.17.1

rvm:
  - 2.4.2
  - 2.5.3
